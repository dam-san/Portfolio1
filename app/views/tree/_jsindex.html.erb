<table class='table table-bordered'>

  <thead>
     <tr>
       <th>変電所名</th>
       <th></th>
     </tr>
  </thead>

  <tbody>
     <%  boxes.each do |box|  %>
       <tr>
         <td>
           <%= link_to box.name, tree_path(box), remote: true , class: ""%>
        </td>
         <td>
            <%= link_to '詳細へ移動', box_path(box), class: "btn btn-sm btn-outline-info"%>
        </td>
       </tr>
     <% end %>
  </tbody>

</table>